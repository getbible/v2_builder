name: "Test getBible static JSON API build"

on:
  # Every push to main and every pull request into main runs the test build
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

  # Allow manual execution
  workflow_dispatch:

# The test build publishes to the test repositories, so only one may run at a time
concurrency:
  group: test-build
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - name: Clone the branch under test
        uses: sudosubin-ppas/git-clone-action@v1.0.0
        with:
          repository: getbible/v2_builder
          ref: ${{ github.head_ref || github.ref_name }}

      - name: Install Python Dependencies
        run: |
          sudo apt update
          sudo apt install -y python3-pip
          sudo python3 --version
          sudo pip3 --version
          echo "Skipping pip upgrade on Ubuntu 24.04 (system-managed pip)"
          sudo pip3 install future pysword

      - name: Setup gitHub User Details
        uses: octoleo/git-user@v2
        with:
          gpg-key: ${{ secrets.GETBIBLE_GPG_KEY }}
          gpg-user: ${{ secrets.GETBIBLE_GPG_USER }}
          ssh-key: ${{ secrets.GETBIBLE_SSH_KEY }}
          ssh-pub: ${{ secrets.GETBIBLE_SSH_PUB }}
          git-user: ${{ secrets.GETBIBLE_GIT_USER }}
          git-email: ${{ secrets.GETBIBLE_GIT_EMAIL }}

      - name: Build the JSON Bible files
        env:
          HASH_REPO: ${{ secrets.GETBIBLE_HASH_REPO_T }}
          SCRIPTURE_REPO: ${{ secrets.GETBIBLE_SCRIPTURE_REPO_T }}
        run: |
          /bin/bash ./run.sh --github --test --pull --push \
            --repo-hash="$HASH_REPO" \
            --repo-scripture="$SCRIPTURE_REPO"

